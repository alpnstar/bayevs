import React, {FC} from 'react';
import Slider from "../Slider/Slider";

const Home: FC = () => {
    return (
        <div className="home">
            <Slider/>
        </div>
    );
};

export default Home;